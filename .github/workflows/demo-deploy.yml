name: Demo auto-deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: demo-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy demo stack
    runs-on: ubuntu-latest
    environment: demo
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy over SSH
        run: bash ./deploy/demo/demo-deploy.sh
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_PORT: 22
          SSH_KEY: ${{ secrets.SSH_KEY }}
          SSH_HOST_KEY: ${{ secrets.SSH_HOST_KEY }}
          SSH_USER: ec2-user
          REPO_PATH: /home/ec2-user/aggregator-demo/repo
          DEPLOY_BRANCH: ${{ github.ref_name }}
          ADMIN_USERNAME: ${{ secrets.ADMIN_USERNAME }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
          FEEDBACK_STORAGE_CONFIG: '{"type":"dynamo","dynamo":{"table_name":"aggregator_feedback","aws_region":"eu-central-1"}}'
