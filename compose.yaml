services:
  aggregator:
    build:
      context: .
      dockerfile: services-core/aggregator/Dockerfile
      args:
        CONTAINER_ROOT: services-core/aggregator
        CATALOG_FILE: config/empty/catalog.yaml
        HEALTH_CHECKS_FILE: config/empty/health-checks.yaml

  aggregator-ui:
    build:
      context: .
      dockerfile: services-core/aggregator-ui/Dockerfile
      args:
        CONTAINER_ROOT: services-core/aggregator-ui
        CATALOG_FILE: config/empty/catalog.yaml
        VITE_APP_TITLE: ""

  prometheus:
    image: prom/prometheus:v2.54.1
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=1h"
      - "--storage.tsdb.retention.size=50MB"
    volumes:
      - ${PROMETHEUS_CONFIG_FILE:-./services-extra/prometheus/prometheus.yml}:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus

  grafana:
    image: grafana/grafana:12.3.1
    environment:
      GF_SECURITY_ALLOW_EMBEDDING: "true"
      GF_SERVER_ROOT_URL: "%(protocol)s://%(domain)s:%(http_port)s/grafana"
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
    volumes:
      - ${GRAFANA_PROVISIONING_DIR:-./services-extra/grafana/provisioning}:/etc/grafana/provisioning:ro
      - ${GRAFANA_DASHBOARDS_DIR:-./services-extra/grafana/dashboards}:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana

volumes:
  prometheus-data:
  grafana-data:
