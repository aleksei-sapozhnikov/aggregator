global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: aggregator
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - aggregator:8080
