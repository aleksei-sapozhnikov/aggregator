server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus

catalog:
  path: file:./catalog.yaml

health:
  checks-path: file:./health-checks.yaml
