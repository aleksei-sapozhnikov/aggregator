# Demo environment: prod-like runtime settings, but uses demo catalog and demo checks.
catalog:
  path: classpath:demo/catalog.yaml

health:
  checks-path: classpath:demo/health-checks.yaml

management:
  endpoint:
    health:
      show-details: never
