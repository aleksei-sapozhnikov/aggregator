# Demo environment: prod-like runtime settings, but uses demo catalog and demo checks.
catalog:
  path: file:./config/demo/catalog.yaml

health:
  checks-path: file:./config/demo/health-checks.yaml

management:
  endpoint:
    health:
      show-details: never
