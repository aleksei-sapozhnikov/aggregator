server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - prometheus

catalog:
  path: classpath:catalog.yaml

health:
  checks-path: classpath:health-checks.yaml
